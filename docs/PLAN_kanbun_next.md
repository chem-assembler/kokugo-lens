# 「返り点でみる漢文」これから何をするか（2026-08-13・v51 時点）

台材の消化が終わった直後に書いた計画。**この文書だけを読んで作業を始められる**ように、
現在地・やること・落とし穴・コマンドをすべて書いてある。

## 現在地

**数字は README の記憶ではなく、いつも機械で出し直すこと。**

```bash
node kanbun/tests.js            # 725/725（回帰テスト。合格が収録の絶対条件）
node kanbun/kuho-check.js       # 型と実例の往復。結び付いた問題数もここに出る
node kanbun/yomi-check.js       # 読み仮名。二重帳簿の死角を見る唯一の検査
node kanbun/tools/uncollected.js catalog   # 台帳の残り
```

- **678問 / 69作品**（春秋左氏伝・孫子が加わった）
- 台帳 `docs/material-catalog.md` は **498行中 495行**が収録済み ＝ **消化しきり**
  （残る3行はどれも「入れない理由がある」行。下の §1 参照）
- 型 43種すべてに実例あり。**結び付いた問題は 325 / 678**／実例1件の型は 9 → **1**（残る1型は「素材が無い」で決着）
- 熟語返り（連読符）は **7トークン / 6問**（v47 で画面に描くようにし、v49「大赦天下」・v51「厚遇戦士」を足した）

---

## 1. `H-04f` を収録する（1問・半日）→ **済（2026-08-13・v44）**

**台帳に残った4行のうち、唯一「入れられる」もの。** ほかの3行は入れない理由がある
（`K-01b` は `mujun-2` と重なる／`S-36` は前半後半を別々に収録済み／
`S-65e` は「〜者は」で切れる従属節）。

- 白文: `彊秦之所以不敢加兵於趙者、徒以吾兩人在也`（19字。長いので**後半で切る**）
- 収録した形: `徒以吾両人在也` →「徒だ吾が両人の在るを以てなり」（id `funkei-tada`）
- 訓点は `徒ダ 以テ二 吾ガ 両 人ノ 在ルヲ一 也`。限定の副詞は先に読むので「徒」に返り点は付かない
- 型 `gentei-tada` に結んだ。結ぶために型データへ標識「徒」と読み「徒だ」を足している
  （`markers` に「徒」が無く `link-verify.js` の規則10で弾かれたため）

### この節の当初の目当て「熟語返り」は達成できていない

もとの計画は「H-04f を**熟語返りで**組む」だったが、**連読符は入っていない**。理由:

- 連読符の当てどころだった **「所以」は切り落とした前半にしかない**。後半 `徒以吾両人在也` に
  二字で一語として返る箇所はひとつも無い
- そもそも**このリポジトリは「所以」を連読符で組んでいない**。既存5件
  （`kanpeki-yuen` / `suishi-koryu` / `suishi-keitai` / `shisetsu-yuen` / `kiro-kaeru`）は
  すべて **所=二・以=三 の二・三点**で組んである。新規だけ連読符にすると流儀が割れる

→ **熟語返りの不整合は §2 に持ち越した。** 素材から集め直す仕事なので実質 §2 と同じ性質。
その後 v47 で画面に描くようにし、v49「大赦天下」（史記 高祖本紀）・
v51「厚遇戦士」（史記 廉頗藺相如列伝・李牧）を足して **6問7トークン**。
`大破`・`大赦`・`厚遇` 以外の「二字の動詞熟語＋目的語」を探し続ける。

既存の熟語返りの実例（形をまねる）:

| id | 白文 | 書き下し |
|---|---|---|
| `shiki-taiha-sogun` | 大破楚軍 | 楚軍を大破す |
| `shiki-taiha-shingun` | 大破秦軍於東阿 | 秦軍を東阿に大破す |

> **`shiki-taiha-shingun` の返り点は誤りだった → 直した（v45）。** 大=下（lv2 n3）・阿=上（lv2 n1）で、
> **内側に一二点がひとつも無いのに上下点を使っていた**。上下点は一二点をまたぐときの印なので、
> 阿=一・大=二 が定石。note の「上下点と置き字が同居する上級の形」も書き換え、難度は 7.8 → 5.8 になった。
> 読み順は上下点でも正しく出るため**二重帳簿は素通りしていた**。全667問でこの1件だけ。
> 同じ抜けを塞ぐ検査を `tests.js`（収録済み全問）と `tools/lane-verify.js`（収録前）に入れてある。

> **`join`（連読符）は画面に描かれていなかった → 描いた（v47）。** それまでは「大破秦軍」が
> `大二 破ス 秦 軍ヲ一` としか見えず、なぜ大→破と続けて読むのかが画面から読み取れなかった。
> いまは字と字の境をまたぐ朱の縦棒（`.joiner`）が出て、モードの操作説明にも一文が足される。
> **描けるのは相手が隣の字のときだけ**なので、その前提を `tests.js` と `lane-verify.js` で固定してある。
> ＝ 熟語返りの実例を増やせば、増やしたぶんがそのまま画面に出る状態になった。

手順は `lane-verify.js` → `lane-merge.js` → 実機 L1 で完答、といつもどおり。

---

## 2. 実例が1件しかない型9つを厚くする（数日）

型の解説から訓点モードへ渡すカードが**1枚しかない**型が9つある。

**9型のうち8型を埋めた（v48〜v50）。残る1型は「素材が無い」という結論で決着（v51）。**

| 型 | 名前 | 足したもの | 件数 |
|---|---|---|---|
| ~~`hitei-zenbu`~~ | 全部否定（副詞＋不） | 楚魏以趙為媾必不救王（史記） | 2（v50） |
| ~~`hitei-hizunba`~~ | 否定条件（非A不B） | 非利不動非得不用非危不戦（孫子） | 2（v49） |
| ~~`shieki-doushi`~~ | 命令の動詞による使役（命・勧） | 命子封帥車二百乗以伐京（左氏伝） | 2（v48） |
| ~~`ukemi-tokoro`~~ | 「為A所B」の受身 | 先即制人後則為人所制（史記） | 2（v48） |
| ~~`gentei-ruika`~~ | 累加（不唯A亦B） | 非独政能也乃其姉亦烈女也（史記） | 2（v49） |
| ~~`gentei-sugizu`~~ | 限定（不過〜にすぎず） | 鷦鷯巣於深林不過一枝（荘子） | 2（v50） |
| ~~`yokuyo-nao`~~ | 抑揚（A猶B、況C乎） | 蔓草猶不可除…（左氏伝）／布衣之交尚不相欺…（史記） | 3（v48） |
| ~~`eitan-anizuya`~~ | 詠嘆（豈不A哉） | 豈不謬哉（史記 項羽本紀） | 2（v49） |
| `eitan-nanzosore` | 詠嘆（何其A也） | — **素材が無いと結論**（下記） | **1（据え置き）** |

### `eitan-nanzosore` は **(a) 1件のままにする**（2026-08-13・ユーザー判断／「とりあえず」）

**底本の句読点で見るかぎり「！」で終わる用例が見つからない。**
五代史伶官伝序「何其衰也**！**」（＝いまの唯一の実例）以外は、史記 卷047 の2か所・
莊子 齊物論・孟子 盡心上／盡心下・論語 子罕 のいずれも「？」だった（一覧は
`docs/material-kuho-9types.md`）。**句読点で疑問と詠嘆を分ける規律をそのまま通し、
「用例が無い」ことを結論として残す**ことにした。

**切り替える余地は残してある。** 規律をゆるめれば孟子 盡心上「何其声之似我君也」（8字）が
入るが、そのときは**疑問の型との見分けが「？か！か」でなくなる**ので、型の note に
見分け方を書き足す必要がある。必要なもの（候補の白文・字数・出典）は記録に書いてある。

**全部否定は「短文が乏しい」という見立てが外れた**（史記だけで8巻に「必不」の用例がある）。

→ **これで §2 は打ち止め。** 残りは熟語返りの上積みだけ。

### まず素材が無い、というのが出発点

**台帳は2つとも消化しきった**（`material-catalog.md` 494/498・`material-minqing.md` 58/61)。
残りはどれも「入れない理由がある」行なので、**新しい白文を集めるところから始まる**。

### 前例をなぞる（`docs/material-kuho-4types.md`）

2026-08-12 に同じことを一度やっている。**8候補中4件しか採れなかった**という記録が
そのまま残っているので、必ず先に読むこと。落ちた4件の中身:

- **2件は原文に存在しなかった**（報告は「確度: 確実」と自己申告していた）
- 1件は「常」が名詞で句形が別物
- 1件は底本の句読が疑問符で、詠嘆ではなかった

さらに**訓点欄は4件中3件が誤っていた**（返り点が1つ抜けている／二点と一点が逆／
最初に読む字に返り点を付けている）。**訓点は必ず自分で組んでエンジンに通す。**

### 段取り

1. ~~型ごとに「どういう形を探してほしいか」を書いた依頼パックを作る~~
   → **済。`docs/gemini-pack-kuho-9types.md`（2026-08-13・v47）。**
   9型ぶんに加えて**熟語返り（連読符）の依頼も同じパックに入れてある**（§1 で積み残したぶん）。
   前回落ちた4件の落とし方を表にして冒頭に置き、**返り点は書かせない**ことにした
   （前回は報告の訓点欄が4件中3件で誤っていた）。代わりに
   **全文が読める URL と底本の句読点を必須**にしている（不在2件・疑問／詠嘆1件はこれで防げる）
2. Gemini に投げ、`scratch/gemini-kuho-9types.json` に出させる（`docs/` には置かせない）
   → **第1回は投げなかった。** 維基文庫の生テキスト（`action=raw`）に直接あたって集めるほうが
   速く、落ちる情報も無いと判断した（前回は8件中4件が検収で落ち、そのすべてが
   「原文にあたれば1回で分かる」誤りだった）。**依頼パックは残してある**ので、
   自力で見つからない型が残ったら投げればよい
3. **白文と出典だけを採る。** 訓点・書き下しは `gemini-intake.js` に組ませるか手で組む
4. `lane-verify.js` → `lane-merge.js` → `link-verify.js` で型に結ぶ → 実機 L1

型あたり2〜3件入れば「1件だけ」は解消する。**9型 × 2件 = 18問**が目標。
熟語返りは別枠で3〜5件（「二字の動詞熟語＋目的語」の形だけが該当する。理由は
`docs/material-renshokufu.md` の検収結果）。

### 進み具合（正は `docs/material-kuho-9types.md`）

- **第1回（v48）** 4問・3型。抑揚 1→3件（「猶」の形と「尚」の形が両方そろった）／使役 1→2件／受身 1→2件
- **第2回（v49）** 4問。否定条件 1→2件／累加 1→2件／詠嘆（豈不）1→2件＋熟語返り1件
- **第3回（v50）** 2問。不過 1→2件／全部否定 1→2件
- **第4回（v51）** 熟語返り1問（厚遇戦士）。**「何其A也」は (a) 1件のままで決着 ＝ §2 は打ち止め**

**残っているのは熟語返りの上積みだけ。**
探し方は維基文庫の検索 API（`insource:` ＋ `prefix:`）。**`OR` でつなぐと 0 件になる**ので1語ずつ。

---

## 3. `/qa/`（一問一答）の拡充

**6単元・155項目**（v6 で 116→131・v7 で 131→143・v8 で 143→155）。漢文アプリが 678問なのに対してまだ薄い。

手順は `docs/HOWTO_add_problems.md` ではなく、一問一答の量産手順
（単元パックJSON → 機械検証 → マージ → 版バンプ → 実機 → コミット）に従う。

- `node qa/check.js` が通ること（**二面構成＝flip と choice の両方**が必須。片面だと
  測定モードでめくりに落ちる）
- **`qa/questions.json` は `JSON.stringify(d, null, 2) + '\n'` が不動点**。
  この形で書き戻せば追記が純粋な追加行になる（第1回は 514 行の追加・削除 0 行）
- 版番号は **`qa/index.html` の2か所と `qa/app.js` の1か所**（`questions.json?v=NN`）。
  `qa/app.js` は .html しか見ない検査の死角なので、**必ず grep で取りこぼしを確かめる**
- 実機は `?from=kanbun&code=<コード>` で**その1項目だけのめくり回**を開ける。
  新項目の検算はこれが速い（一覧から引き当てるより確実）

### 第1回（2026-08-13・v6）— 15項目

**穴は「漢文アプリ側との対応」で機械的に洗った。**

- **句法 38 → 43。** `kuho.json` の43型のうち5型が `/qa/` に無かった
  （`hitei-arazu` / `hitei-atawazu` / `hitei-hizunba` / `hango-nya` / `gentei-sugizu`）。
  **これで 43 型 ⇄ 43 項目でちょうど揃った**（片側だけの型は0）
- **出典と人物 13 → 23。** 収録69作品に対して13項目しかなかった。
  諸子百家に 列子・淮南子・呂氏春秋・孫子、**新しい行「名文」**に
  出師表・桃花源記・陋室銘・愛蓮説・岳陽楼記・雑説（馬説）を足した

### 第2回（2026-08-13・v7）— 12項目

**穴は「白文に多く出る字」と「解説の粗さ」で洗った。**

- **語彙 26 → 32。** 白文の字を数え直して、上位60字のうち項目が無いものを拾った
  （人称代名詞・相・見・欲・然・足）。`node -e` で数えるだけで出る:
  ```bash
  node -e "const t=require('./kanbun/texts.json');const f=new Map();for(const p of t.problems)for(const k of p.tokens)f.set(k.c,(f.get(k.c)||0)+1);console.log([...f].sort((a,b)=>b[1]-a[1]).slice(0,60).map(x=>x[0]+x[1]).join(' '))"
  ```
- **出典と人物 23 → 29。** 漢詩の読み方が `kbn.shu.kishi`（絶句／律詩・五言／七言）
  1項目しかなかったので、押韻・対句と聯の名・起承転結・句切れ（五言 2+3／七言 4+3）を足した。
  あわせて四書五経と『礼記』も（『大学』『中庸』がもと礼記の一篇、という筋を通すため）

### 第3回（2026-08-13・v8）— 12項目

**穴は「収録データの `source.chapter` が持っている作者名」で洗った。**
`texts.json` の章名には `李白・七絶`・`王士禛・巻二十一` のように**作者が入っている**ので、
文学史の項目はそこから裏を取れる（外の記憶に頼らない）。

- **出典と人物 29 → 37。** 詩は作品ごとではなく**作者と代表作の組**でまとめた
  （李白・杜甫・王維・その他7人）。ほかに 蘇軾・唐詩紀事（推敲）・説苑・
  明清の筆記の著者（王士禛／謝肇淛／銭泳／張潮）
- **訓点の基礎 14 → 16。** 「書き下しの決まり」の行が1項目しか無かったので、
  平仮名に直す代表的な字（不・之・也・可・如／若・使・無・而已）と句読点を足した
- **語彙 32 → 34。** 「道（いフ）」「後（おくル・のちニす）」。どちらも収録済みの白文に実例がある

**`qa/check.js` に「文面に Markdown の `**` を混ぜない」検査を足した。**
`app.js` は q / a / supplement / options をすべて `esc()` に通すので、
`**強調**` と書くとアスタリスクがそのまま画面に出る（漢文アプリ側の note で
実際に55問やらかした問題と同じ）。今回の起草で1件混ざり、この検査で止まった。

### 次に埋めるなら

- **訓点の基礎は難度1が7つ**とまだ下に偏っている
- 日本漢文（頼山陽『日本外史』『題不識庵撃機山図』・広瀬淡窓『桂林荘雑詠示諸生』）が未収録
- 『漢書』『蘭亭集序』『五代史伶官伝序』『黔之驢』も個別項目が無い
- 単元そのものを増やす手もある（**故事成語**は項目として独立していない）

---

## 踏んではいけない穴（すべて 2026-08-13 に実際に踏んだ）

1. **返りを全部収録し終えるまで、依頼パックを作り直さない。**
   `gemini-pack.js` はそのときの未収録行を数え直して束に割るので、
   途中で作り直すと2束目以降がそっくり入れ替わり、**どの束にも入らない行が出る**。
   S系36行が宙に浮き、消化しきったつもりで終わるところだった
2. **字体の対応表（台帳 §8.1）の注意書きをコードフェンスの中に書かない。**
   表を読む正規表現が矢印の前後1字を拾うので、「鞦韆→鞦韆(そのまま)」という
   注記から**「韆→鞦」という対応を作ってしまい**、白文が「鞦鞦」になった
3. **機械検査を通ることと、教えとして正しいことは別。**
   1束目36件は機械を 36/36 通ったが、定訓と照らすと30箇所直した。
   「獣すら相食む」（正しくは「獣の相食むすら」）のように**意味が変わる誤り**も混じる
4. **読み仮名（yomi）は書き下しに出ないので、送り仮名の割り直しでは直らない。**
   `yomi-check.js` が最後の砦。収録後に落ちたら直す
5. **返り点は `gemini-intake.js` が組む。外に組ませない。**
   階層の割り当てに2度不備が出ており（待っている字が別の組の「一」で巻き添え解放される／
   起点が待機中の字を兼ねる）、どちらもエンジンの規則を知らないと踏む
6. **`note` の `**` はもう画面に出ない（2026-08-13・v46 で対処済み）。**
   それまでは `innerHTML` に素の文字列を差し込んでいたので、55問でアスタリスクが
   そのまま表示されていた。いまは `Kanbun.noteHtml()` が `<strong>` に変える。
   **強調は `**…**` で閉じること**（閉じ忘れは `lane-verify.js` が止める）。
   なお **note に内部の問題 id を書かない**（生徒に `funkei-ryouko` は意味を持たない）。

## 道具（`kanbun/tools/`）

| ファイル | 何をするか |
|---|---|
| `kyujitai.js` | 台帳から字体の対応表を読む。**表は持たない**（二重帳簿を作らない） |
| `uncollected.js` | 台帳の未収録行を出す |
| `gemini-pack.js` | 未収録行から依頼パックを組む。`node … <接頭 K/S/H/P/B> [1束の行数]` |
| `gemini-intake.js` | 返りを問題データに組み直す。**order と返り点を作り、エンジンで突き合わせる** |
| `lane-verify.js` | 収録前の1レーン検査 |
| `lane-merge.js` | レーンをまたぐ事故（id 衝突・白文の二重収録）を見る。`--write` で収録 |
| `link-verify.js` | 型と問題の結び付けを検査。`--write` で `kuho.json` へ |
