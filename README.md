# 国語レンズ（KokugoLenz）

SchoolLenz の第3の教科サブブランド。
**公開中: https://koku.schoollenz.com/** （2026-08-11 公開）
リポジトリ: `chem-assembler/kokugo-lens` ／ GitHub Pages（main / ルート）／ HTTPS 強制済み

## 構成

- ルート `index.html` … 国語レンズのハブ（アプリ一覧。自己完結・外部アセットなし）
- **`/kanbun/`** … 返り点でみる漢文（第1弾アプリ）
- **`/qa/`** … 一問一答（漢文）。**chem の `/qa/` エンジンをそのまま流用**し、
  台帳（`questions.json`）だけ漢文用に差し替えた。`node qa/check.js` が台帳を検査する
- `DESIGN_kanbun.md` … 設計書（決定事項・データモデル・アルゴリズム・実装順序の正）
- `PUBLISH_CHECKLIST.md` … 公開手順（GitHub Pages ＋ koku.schoollenz.com）
- `docs/` … 題材選定の資料（下記）

## 返り点でみる漢文 `/kanbun/`

| ファイル | 責務 |
|---|---|
| `kanbun.js` | 中核ロジック（DOM 非依存）。読み順計算 `readOrder` / 書き下し生成 `toKakikudashi` / 採点 `grade` / 難易度 `difficulty` / 仮名正規化 |
| `game.js` | 訓点モードの UI（縦書き描画・訓点パレット・ライブ書き下し・**5モード L1/L2/L3/L4/L6**・誤答再生アニメ） |
| `kuho.js` + `kuho.html` | 句法クイズ（3形式） |
| `guide.js` + `guide.html` + `universities.json` | **志望校から選ぶ案内**（19大学・方式）。志望校を選ぶと「漢文がどう出るか」と「このアプリのどこをやると効くか」が出る。データは `docs/exam-kanbun-sources.md` の実測調査から起こしたもので、**推測は書かない**。`node kanbun/guide-check.js` が検査（遷移先の実在・モード名の一致・強調記法の閉じ・出題実績には調査範囲を添えているか） |
| `kudashi.js` + `kudashi.html` | 書き下し練習（カードD&D）。**訓点を与えず白文だけで読み順を組む＝ L5 に対応**（東北大 第四問②・早稲田 文/法の形） |
| `sortable-lite.js` | 並べ替え。**InfoLens から流用**し、掴む対象を `opts.itemSelector` で指定できるよう拡張 |
| `progress.js` | **学習履歴**（DOM・localStorage 非依存）。訓点モード L1/L2/L3/L4/L6 と書き下し練習(K)の**6モード**で1つの記録を共有する。`localStorage` が使えない環境ではメモリ上で動く |
| `texts.json` | 問題データ（トークン列・訓点・正解の読み順・書き下し）。現在133問（日本漢文12問、**明清の筆記60問**、熟語返り2問）。**11句法すべてに例文あり**。`note` は帰属の疑義・読みの落とし穴を書く欄で、クリア後に表示される |
| `kuho.json` | 句形の型データ（38型・11カテゴリ）。**全38型に例文あり・問題側は 107/133**。`node kanbun/kuho-check.js` が検査（実例が本当にその型か＝白文の標識・書き下しの読み・カテゴリタグの往復まで見る） |
| `test.html` + `tests.js` | 回帰テスト。**コミット前に全合格必須**。`node kanbun/tests.js` でも走る |

## 題材選定の資料 `docs/`

| ファイル | 中身 |
|---|---|
| `material-catalog.md` | **白文素材台帳**。PD の一次テクスト（ほぼ全件 Wikisource）から約610行・約100作品。句法逆引き索引つき。訓点・書き下し・現代語訳は収集していない（アプリ側で自作するため） |
| `material-minqing.md` | **明清の筆記の白文台帳**。61行・9作品。**共通テスト最頻ジャンル**（26件）の素材。返り点の型から引ける逆引き索引つき。所引の行や底本の異同も注記済み |
| `exam-kanbun-sources.md` | **入試出典データ**。共通テスト/センター1979〜2026、**主要7大学（東大・東北大・阪大・名大・九大・神戸大・北大）の年度別出典を全年確定**、広島・お茶の水・筑波、早稲田2020〜2026、私大は漢文の有無を20大学・約70方式で確定。**漢文を出さない大学（京大・一橋・都立大）も確定済み**。事実データのみで問題本文は含まない |
| `FEASIBILITY_kobun.md` | **古文へ横展開できるかの見極めメモ**（2026-08-12）。設計書ではない。「漢文の返り点に当たる一点は何か」だけを判定した結果、**一点は「接続」**で、帳簿が漢文の1本に対し3本立つと分かった（連結→原文／助動詞の接続＝直前の活用形／活用表→語形の復元）。実データ2件＋わざと壊した対照1件で検証済み。**弱点は「返り点は書く操作、接続は見抜く操作」という違い**で、そこをどう埋めるかも書いてある |
| `PLAN_kanbun_recording.md` | **録画モードを入れるための調査結果と手順書**（2026-08-12・調査のみでコードは未変更）。化学の収録CLI（`tools/record/`）は**1行も触らずに使える**と分かった。アプリ側に足す受け口は6か所。**情報レンズとの差が2つ**あり、(1) InfoLens は `window.dnclApp` を元から持っていたが漢文の `game.js` は完全な IIFE で何も公開していない、(2) モバイル判定が 700px なので収録の 810×1440 では当たらず PC の2カラムで撮れてしまう。**地雷**: 公開グローバルを `window.game` と名付けると収録の最後に落ちる。見積もりは合計1日・3コミット。最初に撮る3本の案（百聞不如一見×L1／温故而知新×L3／少年易老学難成×L3の誤答）も入っている |

**この2つの使い方**: `exam-kanbun-sources.md` の優先題材リストで「何を入れるか」を決め、
`material-catalog.md` の行IDで「その白文を引く」。訓点と書き下しは自作し、
`tests.js` の二重帳簿検査（訓点→読み順→書き下し）を必ず通す。

起動: リポジトリルートでローカルサーバーを立てる（例: `python -m http.server 8138` →
`http://localhost:8138/kanbun/`）。file:// 直開きは texts.json の fetch が失敗する。

## 実装状況（設計書 §10 の実装順序に対応）

- [x] 1. `readOrder()` ＋テスト（レ点・連鎖・一レ合字・一二三・上中下・甲乙・再読文字・熟語返り・置き字）
- [x] 2. `toKakikudashi()` ＋二重帳簿テスト（全問題で 訓点→読み順→書き下し を機械検証）
- [x] 3. 縦書き表示（CSS `writing-mode: vertical-rl`。SVG は使わない＝設計書 §6.1）
- [x] 4. 訓点パレット＋ライブ書き下し（L3 が遊べる状態）
- [x] 5. L1（読む順にタップ）・L2（1箇所だけ4択。誤答肢は読み順シミュレーションで「偶然正解になる肢」を除外）
- [x] 6. 誤答アニメ（SVG 矢印レイヤーでユーザーの読み順を1字ずつ辿り、食い違い位置で停止・強調。正解の読み順も再生可）
- [x] 7. `kuho.json` ＋句法クイズ（38型・11カテゴリ・3形式。`node kanbun/kuho-check.js` がデータ検査）
      v18 で**文末の疑問（乎・与・邪）・文末の詠嘆（A 哉・A 夫）・比況（如・若・猶）**を追加。
      解説の「収録例」は白文・書き下し・出典のカードで出し、押すと訓点モードの
      その問題が開く（`index.html?p=<問題ID>`）
- [x] 8. 書き下し練習（カード D&D。`sortable-lite.js` を InfoLens から流用）
      白文カードを「読む順」へ並べる。置き字は「読まない箱」へ、再読文字は［＋］でカードが2枚になる。
      **K2（送り仮名を選ぶ段）を v12 で実装**（設計 `docs/DESIGN_kudashi_input.md`）。
      カードの「送り仮名？」からボトムシートを開き、4択で選ぶ。問うのは最大5枚。
      候補はデータから導出（活用系列＋corpus）し、**偶然正解になる肢を機械的に除外**する。
      **K3（送り仮名の自由入力）を v17 で実装**。入力欄が主役で、K2 の候補は「候補を見る」の奥。
      訓読の流派差は `acceptable[]` で別解として受け、`validateProblem` が
      「カードの並び・漢字と合わない別解」「標準形と同一視される別解」を機械で弾く。
      段の既定値は自動（一度クリアした問題は次に開くと K1→K2 に上がる。手で選んだら固定）
- [x] **L4（訓点＋送り仮名）**（設計書 §5 の「訓読の完成」。v20 で実装）
      白文だけを見せ、返り点を打ったうえで**送り仮名も選ばせる**。候補は書き下し練習 K2 と
      同じ `okuriChoices`（偶然正解になる肢は機械的に除外済み）を使う。
      **読み順と送り仮名の両方がそろって初めてクリア**（片方だけでは通さない）。
      問う字は K2 と同じく最大5か所（`pickAskable`）。**再読文字は問わない**
      ＝訓点モードのセルは1つ目の送り仮名しか描けないため。二度読みの送り仮名は
      書き下し練習の K2/K3 が受け持つ
- [x] **L6（書き下し文から訓点を復元する逆問題）**（設計書 §5 の最上級。v15 で実装）
      書き下し文を問題文として先に見せ、**送り仮名を隠して**返り点だけを打たせる。
      採点は `grade()` をそのまま使う（読み順で判定する原則は L3 と同じ）。
      クリアすると送り仮名が現れて答え合わせになる
- [x] 9. 基礎知識クイズ（chem の `/qa/` エンジン流用）→ `/qa/` に設置。
      **6単元98項目**（訓点の基礎14・置き字8・再読文字11・頻出語14・出典と人物13・**句法38**）。
      **句法の単元は v4 で追加**し、本体の句法クイズ（38型）と1対1で対応させた。
      起草は Gemini に任せ、`kuho.json` / `texts.json` と機械照合して検収した
      （正解キーの誤り1件・例文の取り違え3件を差し戻さず直した）。
      **全60項目が暗記（flip）と測定（choice）の両方を持つ**（v2 で完了）。
      `qa/check.js` が片面しかない項目を不備として弾くので、以後この穴は再発しない。
      **Lv4（難関）は v3 で 3→13 項目に拡充**し、全5単元に行き渡らせた

## 次にやること（2026-08-12 時点）

**句法の穴埋めは完了**（v8）。11句法すべてに例文が付き、句法クイズの接続も 13/35 → 22/35 に。
**v18〜v22 で型を 35→38 に増やし、接続を 35→65 / 82 問にした。実例のない型は 0 になった**（下表）。

**「未接続」の意味に注意**: 実例が無くても**クイズは出せる**（出題は `kuho.json` の型データ
だけで動く）。無いのは**解説から訓点モードの練習へ渡す橋**だけ。数は `node kanbun/kuho-check.js`
が毎回表示するので、README の記憶ではなくそちらを正とする。

**残っている穴（測定値）**

| 項目 | 現状 | 備考 |
|---|---|---|
| 句法クイズの未接続 | **0型 / 38型**（v22・解消） | 残っていた4型（全部否定・命令動詞の使役・豈不A哉・何其A也）は、Gemini に白文を探させ **8候補中4件を採用**して v22 で閉じた。**落とした4件の内訳と落とし方は `docs/material-kuho-4types.md`**（2件は原文に不在、1件は「常」が名詞で句形が別物、1件は底本の句読が疑問符＝詠嘆でない）。報告は8件すべて「確度: 確実」の自己申告だったので、**原文に当たって確かめる手順を記録に残した** |
| `acceptable[]` の収録 | **4問5件**（v17） | 温ねて/温めて・施すこと勿かれ/施す勿かれ・同にせ/同じくせ/同じうせ・於いて/於て。**確信のある流派差だけ登録する**方針（誤った別解を配るより薄いほうがよい）。K3 では語順と漢字がカードで固定されるので、別解が発生しうるのは送り仮名だけ＝仮名の表記差は `kanaEquals` が吸収済みで、埋めるべき件数は当初の見立てより小さい。未登録の流派差で×になったときのために、K3 の誤答文には「流派差があります。『正解を見る』と見比べて」の注記を出す |
| 志望校案内の未収録 | **調査済みは全件収録**（v21） | 広島大・お茶の水女子大・筑波大を v21 で追加し、`docs/exam-kanbun-sources.md` で調べた大学はすべて案内に載った。**筑波大だけは公式が前期国語を非公開で一次資料の裏取りができない**ため、案内に「他大学より確度が低い」と明記してある。未確認年度（広島2015〜2020・お茶の水2021/2022 ほか）は調査範囲の欄に書いて隠していない |
| 句法の型が足りていない箇所 | **4種**（v26 で判明） | 結び直しの過程で、問題があるのに**受ける型が無い**ことが分かった。否定の「非（〜にあらず）」「不能・不得（不可能）」「非A不B（否定条件）」、限定の「不過（〜に過ぎない）」、そして**文末だけの反語（〜ンや・〜ずや）**。とくに最後は、文末の疑問（`gimon-ka`）と文末の詠嘆（`eitan-kana`）があるのに反語だけ欠けている形で、**1型足せば4問が一度に接続でき**、「文末で見分ける」という論点を教材にできる |
| 読み仮名の自由入力・全部平仮名の書き下し（阪大型） | 未実装 | 採点が書き下し文の照合（論点2）で行われる以上、**漢字トークンの読み仮名は書き下しに現れず採点できない**。全仮名書き下しの照合には字音仮名遣い（てう→ちょう等）の正規化が要る。設計の「決めないこと」7 のとおり K3 の先の課題 |

**明清の筆記は60問**（共通テストの最頻ジャンル26件に対応）。**台帳 `material-minqing.md` は消化しきった**

v23 で20問、v24 で19問、v25 で12問を足し、**台帳61行のうち収録すべき行はすべて入った**
（残る3行は、収録済みの短い切り出しと同じ句が2行と、切り出しが破綻して見送った1行）。
v24 の内訳は 聊斎志異4・閲微草堂筆記1・五雑組5・分甘余話4・池北偶談2・虞初新志2・在園雑志1。
**4レーンの並列で起草し、各レーンが提出前にエンジンで二重帳簿（訓点→読み順→書き下し）を
自分で通す**やり方を採った。**台帳の返り点欄の誤りが計11件見つかり、台帳側も訂正した**（LY-09 の返り先、UC-07 と FG-04 の
副詞を返りの連鎖に入れていた点、YW-06・WZ-03 の「返り点が要らない句を要ると書いていた」点、
WZ-06・UC-04 の記号の選び違い）。**台帳は机上で読むだけでは誤りが出ない。実際に組むと出る。**
FG-03 は切り出しが否定を落として抑揚が成り立たないと分かり、収録を見送った。

**日本漢文は漢詩8問＋散文4問＝12問を収録済み**

- 漢詩: 桂林荘雑詠示諸生（広瀬淡窓）／ 題不識庵撃機山図（頼山陽）
- 散文: **頼山陽『日本外史』巻之一**

**『日本外史』の白文の入手先**: `https://kambun.jp/nihongaishi/`（日本漢文の世界）。
頼三樹三郎の所持本とされる写本の翻刻で、**巻之一〜六の原文テキストが読める**
（`01text.htm`〜`06text.htm`）。ja.wikisource には本文がないので、こちらを使う。
公開は6巻までなので、武田・上杉の逸話（「所争は米塩に在らず」など）は現状取得できない。

未収録の散文（今後の候補）: 佐藤一斎『愛日楼全集』＝名大2022、荻生徂徠『弁名』＝上智2024、
岡本韋庵『鉄鞭』＝広島2023、寺崎蛠洲『蛠洲餘珠』＝九大2026。著作権は全員 PD だが入手経路の調査が要る。

**入試データから見えた実装の含意**（`docs/exam-kanbun-sources.md` §2-7）

- **L3 と L5 は「難易度の階段」ではなく、別々の大学に対応する2つの出口**（§2-7-2）。
  設計書は L5 を「上級者向け」としたが、実際は東北大・早稲田文法の本命形式
  - **L3（返り点を打つ）が効く**: 九大（毎年定番）・早稲田4学部・明治文・法政文・學習院文
  - **L5（白文のみ）が効く**: 東北大（第四問②が看板）・早稲田文法（設問箇所が白文）
  - **守備範囲外と認めるべき**: 名大（本文200〜300字＋150字記述で粒度が合わない）
- **同じ旧帝大でも方針が割れる**。阪大は2021年以降 訓点設問を出さないが九大は毎年出す。
  「難関大対策」と一括りにせず大学ごとに出口を示すほうが誠実
- 共通テストの最頻は明清の筆記（随筆）26件で、史伝は6件のみ。**北大・名大でも先秦の経書は少数**
  （名大は12年間ゼロ）。「受験漢文＝唐宋の史伝」「論語・孟子を押さえれば足りる」は両方とも的を外す
- **一橋志望者に「漢文が出ます」と言ってはいけない**（12年間ゼロ・第二問は近代文語文）。
  ただし訓読調を読む素養としては効くので、訴求するならそう正直に書く

## 白文を `texts.json` に入れるときの仕様（2026-08-05 確定）

台帳（`docs/material-catalog.md` §8）から白文を投入するときの判断はここで固定する。
毎回迷わないための仕様であり、**個別の気分で覆さない**。

### 版本の異同 → **日本の教科書通行形を正とする**

底本（Wikisource は多くが『全唐詩』系）と教科書の通行形がずれる場合、**通行形を採る**。

| 作品 | 底本 | 採用する形 |
|---|---|---|
| 静夜思 | 牀前**看**月光／望**山**月 | 牀前**明**月光／望**明**月 |
| 黄鶴楼送孟浩然之広陵 | 碧**山**尽 | 碧**空**尽 |
| 早発白帝城 | 啼不**尽** | 啼不**住** |
| 前赤壁賦 | 蘇軾自筆真跡 | 通行本 |

理由: このアプリの目的は本文批評ではなく受験・学習。生徒の教科書に「明月光」とある横で
「看月光」を出しても、学習上の利益ゼロで混乱だけが生まれる。決定的なのは
**これらの異同が読み順（＝このアプリが扱う対象）を一切変えない**こと。
ただし「知らずに通行形にした」状態にしないため、**底本の形は `note` に残す**。

### 異体字 → **2つに分けて扱う（一律にしない）**

**(a) 単なる字体差は通行字体に統一する**

`楯→盾` `兔→兎` `靑→青` `无→無` `步→歩` `蜚→飛` `凋→彫` `巵→卮` `蹞→跬`。
読みも意味も変わらないので統一しない理由がない（既存の `mujun-1` が「盾」なので、
統一しないとデータ内で不整合になる）。旧字体→新字体の一括対応表は台帳 §8.1。

**(b) 読みが変わる別字は原文のまま残し、教材にする**

| 字 | 読み | 出てくる場面 |
|---|---|---|
| **亡** | ＝「無」。**「なし」と読む**（「ほろぶ」ではない） | 杞憂「亡處亡氣」 |
| **女** | ＝「汝」。**「なんぢ」と読む** | 臥薪嘗胆「女忘會稽之恥邪」 |

「原文のままだと初学者が読めない」が、**読めないこと自体が学習内容**。
台帳 §8.5「引っかけになる行」にも挙がっており、入試で実際に差がつく。
**読み仮名トグルと `note` で教える**方針にし、置換して潰さない。

### 出典の帰属が疑わしいもの → **「（伝）」と表記し `note` で説明する**

消さずに残し、教科書の記述をそのまま事実と思わせない。
実例: `shonen-1` / `shonen-2`（偶成）は一次典拠がなく朱熹への仮託と考えられるため
`chapter` を「朱熹（伝）」とし、`note` に経緯を書いた。
化学レンズの `qa/KNOWLEDGE_CAVEATS.md`（教科書の単純化を事実と思い込むと事故る）と同じ思想。

## 重要ルール（化学レンズと同じ規約）

- 全ファイル **UTF-8（BOMなし）**。コミット前に Shift_JIS 誤解釈由来の化け文字がないか確認
  （検査パターンの文字自体をファイルに書くと自己検出するので、パターンは chem の CLAUDE.md を参照）
- **このリポジトリでは化け検査が誤検出する**。検査パターンに使う文字はどれも実在の漢字で、
  漢文の作品名・人名に正当に現れる（実例: `彪邨文集`＝岡直養の日本漢文集。「邨」は「村」の異体字）。
  **一致したら機械的に直さず、前後を読んで実在の漢字かどうかを必ず judge すること**
- 採点は**訓点の表記ではなく読み順**で行う（レ点でも一二点でも、読み順が合えば正解＝ variant）
- バージョン `vNN` はコミットごとに `kanbun/index.html`・`kanbun/test.html` のキャッシュバスター＋表示を同時更新
- 1修正=1コミット。ブラウザで実機検証し、検証内容をコミットメッセージに記録
- **コミットメッセージにバッククォートを使わない**。`git commit -m` の中では
  シェルがコマンド置換として解釈し、識別子が黙って空欄になる（2026-08-12 実発生）。
  コード名は裸で書くか、長いメッセージは `-F` でファイルから渡す
- 訓読には流派差があり正解が一意でない（設計書 §4.3）。失敗例を見つけたらテストを足して直す＝**テストが仕様の正**
