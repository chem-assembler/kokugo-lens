# 「返り点でみる漢文」設計書（v0・2026-07-30）

**国語レンズ**（ラテン表記 **KokugoLenz**）＝第3の教科サブブランドの第1弾アプリ。
SchoolLenz の3層ブランド構造・命名規約（〈X というレンズ〉でみる〈Y〉）に従う。レンズ＝**読む順序**。

## 0. 決定事項（2026-07-30 ユーザー決定）

| # | 項目 | 決定 |
|---|---|---|
| U1 | 公開アプリ名 | **「返り点でみる漢文」** |
| U2 | 教科サブブランド | 日本語「**国語レンズ**」／ラテン表記「**KokugoLenz**」（親 SchoolLen**z** の z 署名を継承） |
| U3 | サブドメイン | **koku.schoollenz.com**（chem. / info. と同じ4文字級の短さで揃える） |
| U4 | リポジトリ | 新規 `KokugoLens`（ローカル `マイドライブ\Antigravity\KokugoLens`）。将来「古文」を同ハブ下に足せる |
| U5 | 仮名遣い | **両方許容**。データは歴史的仮名遣いで持ち、現代仮名遣いも正解にする。正しい表記は併記して教える（§4.3） |

未着手の外部作業（ユーザー操作）: GitHub リポジトリ作成 / Pages 有効化 / Squarespace の DNS に koku を追加。
InfoLens で同じ工程が未完のままなので、**先に info を通してから koku を同じ手順で通す**のが安全。

## 1. スコープ

| モード | 中身 | 対応する要望 |
|---|---|---|
| **訓点モード** | 白文に返り点・送り仮名を打つ → 書き下し文を自動出力 | 「簡単操作でレ点を打てる」「自動で書き下し」 |
| **書き下し練習** | 漢字カードを読む順に並べ、読み仮名・送り仮名を付ける | 「カードのドラッグ＆ドロップ」「プリセット適用」 |
| **句法クイズ** | 使役・受身・否定・反語などの型を判定／型を埋める | 「句法クイズ」 |
| **基礎知識クイズ** | 訓点の名称・置き字・再読文字・頻出語・出典 | 「基礎知識クイズ」 |

上級者向け「白文のみ」は独立モードではなく**難易度レベル L5 として訓点モードに内包**する（§5）。
モードを増やさず難易度で深くするのは化学レンズの「アプリ5本キャップ」と同じ判断。

## 2. 中核となる設計原則（化学レンズからの移植）

### 原則1: 表記を比べず、生成される読み順を比べる

Chem-Assembler の「**検証はトポロジーのみ。座標・角度は見た目専用**」と完全に同型。
返り点の打ち方は一意でない（一二点で書ける箇所をレ点で書く、上中下点を使う流派差）。
したがって**採点はユーザーの訓点から計算した読み順（トークン列）と正解の読み順の一致で行い、
訓点の表記そのものは比較しない**。これが本アプリの `isomorphic()` に相当する判定器。

### 原則2: 最大の学習は誤答時に起きる

比例式アプリで確立した方針の移植。誤答時は「×」ではなく、
**ユーザーが打った訓点どおりに読み順を辿るアニメーション**を見せ、
「述語（罔）を先に読んでいます」「一の字に戻れていません」と可視化する。
正答演出より先にこの誤答表示を作る。

### 原則3: データとロジックを分離し、データ側は外部・LLM下書き＋機械検証で埋める

`kanbun.js`（DOM非依存の純粋ロジック）と `texts.json`（作品データ）を分ける。
漢文の原典は著作権フリーで、読み順・書き下しは定型なので、データは下書き→機械検証で量産できる。
**ただし他者が付けた訓点や現代語訳は編者の著作物になり得るので、教科書・参考書からの転記は禁止**。

## 3. データモデル（`texts.json`）

```js
{
  id: 'rongo-gakuji-15',
  source: { work: '論語', chapter: '学而' },
  tokens: [
    { c: '学', yomi: 'まな', okuri: 'ビテ' },
    { c: '而', role: 'placed' },                 // 置き字＝読まない
    { c: '不', yomi: '',     okuri: 'ず',  kana: true, mark: [{ lv: 1, n: 2 }] },
    { c: '思', yomi: 'おも', okuri: 'ハ',  mark: [{ lv: 1, n: 1 }] },
    { c: '則', yomi: 'すなは', okuri: 'チ' },
    { c: '罔', yomi: 'くら', okuri: 'シ' }
  ],
  order: [0, 3, 2, 4, 5],                        // 正解の読み順（トークンindexの列）
  kakikudashi: '学びて思はざれば則ち罔し',
  acceptable: ['学びて思はざれば則ちくらし'],      // 許容する別解（§4.3）
  kuho: ['negation'],                            // 句法タグ（句法クイズと共有）
  meaning: '学んでも考えなければ、はっきりしない'
}
```

### 3.1 早めに決めないと後で壊れる3点

1. **`order` は順列ではなく「index の列（重複可）」**
   再読文字（未・将・当・応・宜・須・猶・盍・且）は**1字を2回読む**（「未ダ〜ず」）。
   順列（各indexが1回）を前提にすると再読文字を入れた時点でモデルが破綻する。
   再読文字は `reread: { first: {yomi,okuri}, second: {yomi,okuri} }` を持ち、`order` に同じindexが2回出る。
2. **置き字は `order` から除外するが、その働きは前の字の `okuri` に入る**
   上の例で「而」は読まないが、その「て」は「学」の送り仮名「ビテ」として現れる。
   これを知らずに実装すると書き下し生成が必ず狂う。
3. **レ点を特殊ケースにしない**
   レ点＝一点と上点の合字。`mark` を**配列**にして
   `[{lv:1,n:1},{lv:2,n:1}]` と表現すれば、レ点のための分岐が消える。
   階層は `lv:1`＝一二三点／`lv:2`＝上中下点／`lv:3`＝甲乙丙点／`lv:4`＝天地人点。
   熟語を跨いで返るハイフン（連読符）は `join: true` でトークンを連結扱いにする。

## 4. `kanbun.js` の API

### 4.1 読み順の計算 `readOrder(tokens) -> number[]`

訓点から読み順を導く。アルゴリズム（左から1回走査＋保留）:

```
出力 = []、保留 = レベルごとの待ち行列
各トークン t を左から:
  t が置き字        → 何もしない
  t に返り点がない  → 出力に追加 → 解放を試みる
  t の最小レベルの番号が 1 → 出力に追加 → 解放を試みる
  それ以外          → 保留に積む（レベル・番号つき）
解放を試みる:
  内側のレベル（lv が小さい方）から順に、
  次に期待する番号の字が保留にあれば出力に追加（これを繰り返す）
  そのレベルが尽きたら外側のレベルへ上がる
```

検算例:

| 白文 | 訓点 | 走査 | 読み順 | 書き下し |
|---|---|---|---|---|
| 読 書 | 読=二, 書=一 | 読を保留 → 書は n=1 で出力 → 二の読を解放 | `[1,0]` | 書を読む |
| 不 敢 食 | 不=二, 食=一 | 不を保留 → 敢は無印で出力 → 食は n=1 で出力 → 不を解放 | `[1,2,0]` | 敢へて食はず |
| 学 而 不 思 則 罔 | 不=二, 思=一, 而=置き字 | 学 → 而は無視 → 不を保留 → 思(n=1) → 不を解放 → 則 → 罔 | `[0,3,2,4,5]` | 学びて思はざれば則ち罔し |

**上中下点・甲乙点・レ点・再読文字の実例は回帰テストで固定する**（`tests.js`）。
訓読は例外が多く、机上でアルゴリズムの完全性を保証できない。
化学レンズと同じく**テストが仕様の正**とし、失敗例を見つけたらテストを足して直す。

### 4.2 書き下し文の生成 `toKakikudashi(tokens, order) -> string`

`order` に沿って各トークンの `yomi + okuri` を連結する。
- `kana: true` のトークンは漢字を出さず仮名だけ書く（助字・助動詞。不→「ず」、之→「の」）
- 再読文字は1回目 `reread.first`、2回目 `reread.second` を使う
- 送り仮名はデータではカタカナ、書き下し出力では平仮名に変換

**二重帳簿による自己検査**: データが持つ `kakikudashi` と、この関数が `order` から生成した文字列が
一致するかを回帰テストで全問チェックする。生成器のバグとデータの誤りが互いに露出する。
`tools/verify-*.js` と同じ「人の記憶に頼らない」道具立て。

### 4.3 採点 `grade(userInput, problem) -> {status, detail}`

3値で返す（比例式アプリの ok / sigfig / wrong と同型）。

| status | 意味 | 表示 |
|---|---|---|
| `ok` | 読み順が正解と一致 | クリア |
| `variant` | 読み順は正しいが訓点の打ち方が登録と違う | **クリアにする**（原則1）。「一二点でも書けます」と併記 |
| `wrong` | 読み順が違う | 誤答アニメ（原則2） |

書き下し文の照合は**正規化してから比較**する。正規化＝カタカナ→平仮名・
歴史的仮名遣い⇔現代仮名遣いの相互変換（ハ行転呼・「ゐゑを」・「ぢづ」）・記号除去。
`acceptable[]` に別解を持たせ、正規化後にどれかと一致すれば正解。
**訓読には流派差があり正解が一意でないのが本アプリ最大の設計リスク**で、
化学の同型判定（数学的に一意）より曖昧である点を認識しておく。

### 4.4 難易度の自動算出 `difficulty(problem) -> number`

問題ごとに `level` を手で書くと必ず矛盾するので**タグから計算**する（比例式アプリの `recommend()` と同型）。

```
基礎 = トークン数 / 4
+ 返り点の最大階層 × 2      （一二のみ=2 / 上中下まで=4 / 甲乙まで=6）
+ 再読文字があれば +3
+ 置き字があれば +1
+ ハイフン（熟語返り）があれば +1
+ 句法タグの難度（否定1 / 使役2 / 受身2 / 疑問2 / 反語3 / 比較3 / 抑揚4 / 仮定3 / 限定3）
```

## 5. 難易度レベル＝足場を段階的に外す（訓点モード）

比例式アプリで確立した「足場を外す三段階」を6段に拡張。**新しい型の初回だけ上の段を出し、
2回目以降は下の段に戻す**（毎回フル組み立ては作業になり離脱要因になる、という既存の知見を適用）。

| L | 与えるもの | ユーザーの操作 | ねらい |
|---|---|---|---|
| **L1** | 訓点つき（完成） | 読む順に字をタップ（番号が付く） | 返り点を知らなくても語順感覚が入る。まず解ける快感 |
| **L2** | 訓点つき（1箇所だけ空白） | その1箇所を4択（レ／一二／上中下／なし） | **学習価値が集中する段。ここが本命の可能性**（比例式の①' と同じ構図） |
| **L3** | 白文＋送り仮名 | 返り点を全部打つ | 訓点を書けるようになる |
| **L4** | 白文 | 返り点＋送り仮名（プリセット候補から選択） | 訓読の完成 |
| **L5** | 白文のみ | 訓点＋書き下しを自力で構成（**上級者向け**） | 入試の実戦形 |
| **L6** | 書き下し文のみ | 訓点を復元する（逆問題・最上級） | 訓点の必然性が分かる |

## 6. UI 設計

### 6.1 縦書きは SVG ではなく CSS で組む

化学レンズは SVG が主役だが、**漢文は `writing-mode: vertical-rl` を使った DOM が圧倒的に楽**
（縦中横・ルビ・折り返しをブラウザに任せられる）。SVG は誤答アニメの矢印レイヤーだけに使う。

1字＝1セル。セル内の配置は grid で固定する。

```
┌──────────────┐
│ ルビ（読み仮名）  │  ← 上（縦書きなので実際は右）
│   漢 字       │
│ 返り点 │送り仮名 │  ← 左下＝返り点、右下＝送り仮名（伝統的配置）
└──────────────┘
```

### 6.2 「簡単操作で返り点を打てる」の具体

字をタップ → その字の下にパレットがポップアップ → 1タップで確定。
- パレット第1段: **一 二 三 ／ 上 中 下 ／ レ ／ ハイフン ／ 消す**
- 番号は**自動割り当てを既定にする**: 「返る」ボタンを押すだけで、
  既に打たれた返り点から次の番号（一→二→三）を自動で決める。
  番号を意識させるのは L2 以上のみ。**初学者に番号を選ばせない**のが「簡単操作」の核心
- 打った瞬間に**書き下し文がライブで更新される**（自動出力の要望はここで満たす）。
  間違った訓点でもそのまま書き下しを出す ＝ 変な文が出ること自体がフィードバックになる
- モバイル前提（muki で確立した可変レイアウト方針を踏襲）

### 6.3 書き下し練習（カードのドラッグ＆ドロップ）

- 漢字カードを縦一列のトレイに並べ替える。**`sortable-lite.js` を InfoLens から流用**
  （外部CDN禁止方針のために自前実装済み。作り直さない）
- 各カードをタップすると読み仮名・送り仮名の入力欄が開く。
  **プリセット適用が既定**（その字の頻出読みを候補ボタンで数個提示）、自由入力も可
- 置き字カードは「読まない箱」へ捨てる操作にする。再読文字カードは**複製できる**
  （2回読む＝カードが2枚になる、という操作でモデルを体で理解させる）
- 完成したら §4.2 の生成器を通し、`acceptable` と正規化照合

### 6.4 誤答の可視化（原則2の実装）

ユーザーの訓点から `readOrder()` を回して得た読み順を、**矢印で1字ずつ辿るアニメ**。
正解の読み順と最初に食い違ったところで停止し、その字を強調して理由を出す
（「ここで一の字に戻る必要があります」）。並べて正解の順路を再生できる。

## 7. 句法クイズ（`kuho.json`）

句形を**有限個の「型」データ**として持つ。化学レンズの `reactions.json`（反応機構の型を有限個手作り）と同型。

```js
{
  id: 'shieki-shimu',
  category: 'shieki',            // 使役
  pattern: '使 A B',             // 型
  markers: ['使', '令', '遣', '俾'],
  kakikudashi: 'AヲシテBセしむ',
  meaning: 'AにBさせる',
  examples: ['rongo-...']        // texts.json の問題ID
}
```

カテゴリ: 否定（単純・二重・部分／全部の区別）・使役・受身・疑問・反語・比較・選択・仮定・限定・抑揚・詠嘆。

出題形式は3つ:
1. 白文の該当部をハイライト → 「何の句形か」4択
2. 型を与えて**書き下しの空欄を埋める**（「未ダ〜○○」）
3. 意味の4択（部分否定「常に〜ず」と全部否定「常に〜ず」の区別など、間違いやすい対を並べる）

**`kuho.json` は訓点モードとデータを共有する**。句形は返り点パターンそのものなので、
句法データが訓点問題の素材を供給する（ion-equation の色データを「色でみる無機」が消費する構図と同じ）。
逆順に作ると二重実装になる ＝ **句法データを先に作り、訓点問題はそれを消費する**。

## 8. 基礎知識クイズ ＝ 既存 `/qa/` エンジンを流用する

新規に作らない。化学で作った一問一答アプリ（コード付き知識項目台帳＋習得マップ、
暗記めくり／測定複数選択の二面構成）は教科非依存の器なので、**項目台帳を漢文用に差し替えるだけ**にする。

台帳に載せる項目: 訓点の名称（返り点・送り仮名・句読点・訓読文・白文・書き下し文）／
置き字（於・于・而・焉・矣・乎）／再読文字9字／頻出語（曰・謂・以・為・之・者・所・如・若・蓋）／
出典と人物（諸子百家・史記・唐詩の作者）。

## 9. ファイル構成（化学レンズの規約を踏襲）

| ファイル | 責務 |
|---|---|
| `kanbun.js` | **DOM非依存の純粋ロジック**。`readOrder` / `toKakikudashi` / `grade` / `difficulty` / 仮名正規化 |
| `game.js` | 訓点モードの UI・訓点パレット・ライブ書き下し・誤答アニメ |
| `kudashi.js` | 書き下し練習（カードD&D） |
| `kuho.js` | 句法クイズ |
| `sortable-lite.js` | InfoLens から流用（並べ替え） |
| `texts.json` / `kuho.json` | 作品データ / 句形の型データ |
| `test.html` + `tests.js` | 回帰テスト。**コミット前に全合格必須** |
| `audit.html` + `audit.js` | 無人監査（全問の読み順計算と書き下し二重帳簿の総当たり＋ランダム操作ファズ） |

規約も同じ: 全ファイル UTF-8（BOMなし）／`?v=NN` キャッシュバスターと表示版を同時更新／
1修正=1コミットで実機検証内容をコミットメッセージに記録／外部アセットゼロ。

## 10. 実装順序（推奨）

1. **`kanbun.js` の `readOrder()` ＋テスト**（返り点の階層・レ点・置き字・再読文字を全部テストで固定）
   ← ここが心臓部。UI より先に、データとロジックだけで正しさを作る
2. `toKakikudashi()` ＋二重帳簿テスト
3. 縦書きの表示だけ作る（訓点つきの文を正しく描ければ半分終わり）
4. 訓点パレット ＋ ライブ書き下し（L3 が遊べる状態）
5. L1・L2 を追加（既に描画とロジックがあるので UI 差分だけ）
6. 誤答アニメ（原則2）
7. `kuho.json` ＋ 句法クイズ
8. 書き下し練習（カードD&D）
9. 基礎知識クイズ（`/qa/` 流用）

段階3までで「動くもの」になり、段階4で公開可能。L5・L6 と句法クイズは後追いでよい。
